{
  "_comment": "What is listening on this machine. system_ports is the set hidden behind `o` — parts of the machine rather than things you started. Reads /proc only, plus `tailscale serve status` for the exposure column.",
  "_schema": {
    "system_ports": { "items": "integer" },
    "refresh": {
      "minimum": 1
    }
  },
  "_system_ports_comment": "Listening ports hidden when system services are hidden.",
  "system_ports": [
    22,
    53,
    123,
    323,
    631,
    5353
  ],
  "_refresh_comment": "Seconds between listening-socket and traffic refreshes.",
  "refresh": 4.0
}
